# Snake-TDD

Migração do jogo da cobra para pygame.

A lógica principal foi mantida em `snake.py`, preservando compatibilidade com os testes desenvolvidos anteriormente.

O arquivo `snake_game.py` implementa apenas a camada gráfica utilizando pygame.
