# Snake-TDD

Migração do jogo da cobra para pygame.

A lógica principal e os testes do pytest foram mantidos em Iteraçao_1 e Iteraçao_2, preservando compatibilidade com os testes desenvolvidos anteriormente.

O arquivo `snake_game.py` implementa apenas a camada gráfica utilizando pygame.
